mutation GenerateSecretSharingUrl(
  $expiresAt: String!
  $passphrase: String!
  $secretsFieldIds: [String!]!
) {
  generateSecretSharingUrl(
    expiresAt: $expiresAt
    passphrase: $passphrase
    secretsFieldIds: $secretsFieldIds
  ) {
    url
  }
}
