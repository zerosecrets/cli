query TeamNames($userId: uuid!) {
  team(
    where: {
      _or: [
        { ownerUserId: { _eq: $userId } }
        { members: { member: { id: { _eq: $userId } } } }
      ]
    }
  ) {
    id
    name
  }
}
