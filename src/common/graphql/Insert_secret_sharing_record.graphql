mutation InsertSecretSharingRecord(
  $expiresAt: String!
  $passphrase: String!
  $secretsFieldIds: [ID!]!
) {
  insertSecretSharingRecord(
    expiresAt: $expiresAt
    passphrase: $passphrase
    secretsFieldIds: $secretsFieldIds
  ) {
    id
  }
}
