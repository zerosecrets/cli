mutation CreateProject($icon: String!, $name: String!, $token: TokenObject, $slug: String!) {
  createProject(icon: $icon, name: $name, token: $token, slug: $slug) {
    id
    tokenValue

    project {
      id
      teamId
      slug

    }
  }
}
