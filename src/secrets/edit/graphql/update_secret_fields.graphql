mutation UpdateSecretFields(
  $id: ID!
  $name: String!
  $slug: String!
  $userSecretFields: [UpdateUserSecretFieldsInput!]!
) {
  updateSecret(fields: $userSecretFields, id: $id, name: $name, slug: $slug) {
    id
  }
}
